# Security

Do not report security vulnerabilities in a public issue.

Email `security@joinaffinityai.com` with a description, affected package version, reproduction
steps, and the impact you observed. Do not include real API keys, patient information, or other
production data.

The SDK is for trusted server-side runtimes. Affinity API keys must not be embedded in browser or
mobile applications.
