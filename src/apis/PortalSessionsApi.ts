/* tslint:disable */
/* eslint-disable */
/**
 * Affinity API
 * Affinity API for software platforms connecting practices to the compounder network. A practice is the customer organization, a provider is an individual clinician or prescriber, and a location is a physical practice site. The API covers practice management, catalog discovery, prescription-order submission, fulfillment tracking, and webhooks.
 *
 * The version of the OpenAPI document: 2026-07-26
 * Contact: support@joinaffinityai.com
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */

import * as runtime from "../runtime";
import {
  type CreatePortalSessionRequest,
  CreatePortalSessionRequestFromJSON,
  CreatePortalSessionRequestToJSON,
} from "../models/CreatePortalSessionRequest";
import {
  type CreatePortalSessionResponse,
  CreatePortalSessionResponseFromJSON,
  CreatePortalSessionResponseToJSON,
} from "../models/CreatePortalSessionResponse";
import { type Problem, ProblemFromJSON, ProblemToJSON } from "../models/Problem";

export interface CreatePortalSessionOperationRequest {
  createPortalSessionRequest: CreatePortalSessionRequest;
  idempotencyKey?: string;
  affinityVersion?: string;
}

/**
 *
 */
export class PortalSessionsApi extends runtime.BaseAPI {
  /**
   * Creates request options for createPortalSession without sending the request
   */
  async createPortalSessionRequestOpts(
    requestParameters: CreatePortalSessionOperationRequest,
  ): Promise<runtime.RequestOpts> {
    if (requestParameters["createPortalSessionRequest"] == null) {
      throw new runtime.RequiredError(
        "createPortalSessionRequest",
        'Required parameter "createPortalSessionRequest" was null or undefined when calling createPortalSession().',
      );
    }

    const queryParameters: any = {};

    const headerParameters: runtime.HTTPHeaders = {};

    headerParameters["Content-Type"] = "application/json";

    if (requestParameters["idempotencyKey"] != null) {
      headerParameters["Idempotency-Key"] = String(requestParameters["idempotencyKey"]);
    }

    if (requestParameters["affinityVersion"] != null) {
      headerParameters["Affinity-Version"] = String(requestParameters["affinityVersion"]);
    }

    if (this.configuration && this.configuration.accessToken) {
      const token = this.configuration.accessToken;
      const tokenString = await token("bearerAuth", []);

      if (tokenString) {
        headerParameters["Authorization"] = `Bearer ${tokenString}`;
      }
    }
    if (this.configuration && this.configuration.apiKey) {
      headerParameters["x-affinity-api-key"] =
        await this.configuration.apiKey("x-affinity-api-key"); // affinityApiKey authentication
    }

    let urlPath = `/v1/portal-sessions`;

    return {
      path: urlPath,
      method: "POST",
      headers: headerParameters,
      query: queryParameters,
      body: CreatePortalSessionRequestToJSON(requestParameters["createPortalSessionRequest"]),
    };
  }

  /**
   * Creates a single-use Affinity Hosted launch URL that expires after 15 minutes. The platform API key must stay on the server.
   * Create portal session
   */
  async createPortalSessionRaw(
    requestParameters: CreatePortalSessionOperationRequest,
    initOverrides?: RequestInit | runtime.InitOverrideFunction,
  ): Promise<runtime.ApiResponse<CreatePortalSessionResponse>> {
    const requestOptions = await this.createPortalSessionRequestOpts(requestParameters);
    const response = await this.request(requestOptions, initOverrides);

    return new runtime.JSONApiResponse(response, (jsonValue) =>
      CreatePortalSessionResponseFromJSON(jsonValue),
    );
  }

  /**
   * Creates a single-use Affinity Hosted launch URL that expires after 15 minutes. The platform API key must stay on the server.
   * Create portal session
   */
  async createPortalSession(
    requestParameters: CreatePortalSessionOperationRequest,
    initOverrides?: RequestInit | runtime.InitOverrideFunction,
  ): Promise<CreatePortalSessionResponse> {
    const response = await this.createPortalSessionRaw(requestParameters, initOverrides);
    return await response.value();
  }
}
