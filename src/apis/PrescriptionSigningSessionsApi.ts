/* tslint:disable */
/* eslint-disable */
/**
 * Affinity API
 * Affinity API for software platforms connecting practices to the compounder network. A practice is the customer organization, a provider is an individual clinician or prescriber, and a location is a physical practice site. The API covers practice management, catalog discovery, prescription-order submission, fulfillment tracking, and webhooks.
 *
 * The version of the OpenAPI document: 2026-07-29
 * Contact: support@joinaffinityai.com
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */

import * as runtime from "../runtime";
import {
  type CreatePrescriptionSigningSessionRequest,
  CreatePrescriptionSigningSessionRequestFromJSON,
  CreatePrescriptionSigningSessionRequestToJSON,
} from "../models/CreatePrescriptionSigningSessionRequest";
import {
  type CreatePrescriptionSigningSessionResponse,
  CreatePrescriptionSigningSessionResponseFromJSON,
  CreatePrescriptionSigningSessionResponseToJSON,
} from "../models/CreatePrescriptionSigningSessionResponse";
import { type Problem, ProblemFromJSON, ProblemToJSON } from "../models/Problem";

export interface CreatePrescriptionSigningSessionOperationRequest {
  createPrescriptionSigningSessionRequest: CreatePrescriptionSigningSessionRequest;
  idempotencyKey?: string;
  affinityVersion?: string;
}

/**
 *
 */
export class PrescriptionSigningSessionsApi extends runtime.BaseAPI {
  /**
   * Creates request options for createPrescriptionSigningSession without sending the request
   */
  async createPrescriptionSigningSessionRequestOpts(
    requestParameters: CreatePrescriptionSigningSessionOperationRequest,
  ): Promise<runtime.RequestOpts> {
    if (requestParameters["createPrescriptionSigningSessionRequest"] == null) {
      throw new runtime.RequiredError(
        "createPrescriptionSigningSessionRequest",
        'Required parameter "createPrescriptionSigningSessionRequest" was null or undefined when calling createPrescriptionSigningSession().',
      );
    }

    const queryParameters: any = {};

    const headerParameters: runtime.HTTPHeaders = {};

    headerParameters["Content-Type"] = "application/json";

    if (requestParameters["idempotencyKey"] != null) {
      headerParameters["Idempotency-Key"] = String(requestParameters["idempotencyKey"]);
    }

    if (requestParameters["affinityVersion"] != null) {
      headerParameters["Affinity-Version"] = String(requestParameters["affinityVersion"]);
    }

    if (this.configuration && this.configuration.accessToken) {
      const token = this.configuration.accessToken;
      const tokenString = await token("bearerAuth", []);

      if (tokenString) {
        headerParameters["Authorization"] = `Bearer ${tokenString}`;
      }
    }
    if (this.configuration && this.configuration.apiKey) {
      headerParameters["x-affinity-api-key"] =
        await this.configuration.apiKey("x-affinity-api-key"); // affinityApiKey authentication
    }

    let urlPath = `/v1/prescription-signing-sessions`;

    return {
      path: urlPath,
      method: "POST",
      headers: headerParameters,
      query: queryParameters,
      body: CreatePrescriptionSigningSessionRequestToJSON(
        requestParameters["createPrescriptionSigningSessionRequest"],
      ),
    };
  }

  /**
   * Creates a one-time Affinity Hosted URL where the mapped provider reviews, PIN-signs, and sends one API-created prescription. The provider PIN never reaches the platform.
   * Create prescription signing session
   */
  async createPrescriptionSigningSessionRaw(
    requestParameters: CreatePrescriptionSigningSessionOperationRequest,
    initOverrides?: RequestInit | runtime.InitOverrideFunction,
  ): Promise<runtime.ApiResponse<CreatePrescriptionSigningSessionResponse>> {
    const requestOptions =
      await this.createPrescriptionSigningSessionRequestOpts(requestParameters);
    const response = await this.request(requestOptions, initOverrides);

    return new runtime.JSONApiResponse(response, (jsonValue) =>
      CreatePrescriptionSigningSessionResponseFromJSON(jsonValue),
    );
  }

  /**
   * Creates a one-time Affinity Hosted URL where the mapped provider reviews, PIN-signs, and sends one API-created prescription. The provider PIN never reaches the platform.
   * Create prescription signing session
   */
  async createPrescriptionSigningSession(
    requestParameters: CreatePrescriptionSigningSessionOperationRequest,
    initOverrides?: RequestInit | runtime.InitOverrideFunction,
  ): Promise<CreatePrescriptionSigningSessionResponse> {
    const response = await this.createPrescriptionSigningSessionRaw(
      requestParameters,
      initOverrides,
    );
    return await response.value();
  }
}
