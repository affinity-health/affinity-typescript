/* tslint:disable */
/* eslint-disable */
/**
 * Affinity API
 * Affinity API for practices and software platforms connecting to the pharmacy network.
 *
 * The version of the OpenAPI document: 2026-08-11
 * Contact: support@joinaffinityai.com
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */

import * as runtime from "../runtime";
import {
  type ArchivePracticeLocationResponse,
  ArchivePracticeLocationResponseFromJSON,
  ArchivePracticeLocationResponseToJSON,
} from "../models/ArchivePracticeLocationResponse";
import {
  type CreatePracticeLocationRequest,
  CreatePracticeLocationRequestFromJSON,
  CreatePracticeLocationRequestToJSON,
} from "../models/CreatePracticeLocationRequest";
import {
  type CreatePracticeLocationResponse,
  CreatePracticeLocationResponseFromJSON,
  CreatePracticeLocationResponseToJSON,
} from "../models/CreatePracticeLocationResponse";
import {
  type GetPracticeLocationResponse,
  GetPracticeLocationResponseFromJSON,
  GetPracticeLocationResponseToJSON,
} from "../models/GetPracticeLocationResponse";
import {
  type ListPracticeLocationsResponse,
  ListPracticeLocationsResponseFromJSON,
  ListPracticeLocationsResponseToJSON,
} from "../models/ListPracticeLocationsResponse";
import { type Problem, ProblemFromJSON, ProblemToJSON } from "../models/Problem";
import {
  type UpdatePracticeLocationRequest,
  UpdatePracticeLocationRequestFromJSON,
  UpdatePracticeLocationRequestToJSON,
} from "../models/UpdatePracticeLocationRequest";
import {
  type UpdatePracticeLocationResponse,
  UpdatePracticeLocationResponseFromJSON,
  UpdatePracticeLocationResponseToJSON,
} from "../models/UpdatePracticeLocationResponse";

export interface ArchivePracticeLocationRequest {
  practiceId: string;
  locationId: string;
  idempotencyKey: string;
  affinityVersion?: string;
}

export interface CreatePracticeLocationOperationRequest {
  practiceId: string;
  idempotencyKey: string | null;
  createPracticeLocationRequest: CreatePracticeLocationRequest;
  affinityVersion?: string;
}

export interface GetPracticeLocationRequest {
  practiceId: string;
  locationId: string | null;
  affinityVersion?: string;
}

export interface ListPracticeLocationsRequest {
  practiceId: string;
  limit?: number;
  startingAfter?: string | null;
  endingBefore?: string | null;
  status?: ListPracticeLocationsStatusEnum;
  affinityVersion?: string;
}

export interface UpdatePracticeLocationOperationRequest {
  practiceId: string;
  locationId: string;
  idempotencyKey: string;
  updatePracticeLocationRequest: UpdatePracticeLocationRequest;
  affinityVersion?: string;
}

/**
 *
 */
export class LocationsApi extends runtime.BaseAPI {
  /**
   * Creates request options for archivePracticeLocation without sending the request
   */
  async archivePracticeLocationRequestOpts(
    requestParameters: ArchivePracticeLocationRequest,
  ): Promise<runtime.RequestOpts> {
    if (requestParameters["practiceId"] == null) {
      throw new runtime.RequiredError(
        "practiceId",
        'Required parameter "practiceId" was null or undefined when calling archivePracticeLocation().',
      );
    }

    if (requestParameters["locationId"] == null) {
      throw new runtime.RequiredError(
        "locationId",
        'Required parameter "locationId" was null or undefined when calling archivePracticeLocation().',
      );
    }

    if (requestParameters["idempotencyKey"] == null) {
      throw new runtime.RequiredError(
        "idempotencyKey",
        'Required parameter "idempotencyKey" was null or undefined when calling archivePracticeLocation().',
      );
    }

    const queryParameters: any = {};

    const headerParameters: runtime.HTTPHeaders = {};

    if (requestParameters["affinityVersion"] != null) {
      headerParameters["Affinity-Version"] = String(requestParameters["affinityVersion"]);
    }

    if (requestParameters["idempotencyKey"] != null) {
      headerParameters["Idempotency-Key"] = String(requestParameters["idempotencyKey"]);
    }

    if (this.configuration && this.configuration.accessToken) {
      const token = this.configuration.accessToken;
      const tokenString = await token("bearerAuth", []);

      if (tokenString) {
        headerParameters["Authorization"] = `Bearer ${tokenString}`;
      }
    }
    if (this.configuration && this.configuration.apiKey) {
      headerParameters["x-affinity-api-key"] =
        await this.configuration.apiKey("x-affinity-api-key"); // affinityApiKey authentication
    }

    let urlPath = `/v1/practices/{practiceId}/locations/{locationId}/archive`;
    urlPath = urlPath.replace(
      "{practiceId}",
      encodeURIComponent(String(requestParameters["practiceId"])),
    );
    urlPath = urlPath.replace(
      "{locationId}",
      encodeURIComponent(String(requestParameters["locationId"])),
    );

    return {
      path: urlPath,
      method: "POST",
      headers: headerParameters,
      query: queryParameters,
    };
  }

  /**
   * Requires locations:write and Idempotency-Key for API keys. Retains the location and historical associations. Archived locations cannot receive new Team assignments. Repeating archive returns the archived location. Changes apply to both Test and Live.
   * Archive location
   */
  async archivePracticeLocationRaw(
    requestParameters: ArchivePracticeLocationRequest,
    initOverrides?: RequestInit | runtime.InitOverrideFunction,
  ): Promise<runtime.ApiResponse<ArchivePracticeLocationResponse>> {
    const requestOptions = await this.archivePracticeLocationRequestOpts(requestParameters);
    const response = await this.request(requestOptions, initOverrides);

    return new runtime.JSONApiResponse(response, (jsonValue) =>
      ArchivePracticeLocationResponseFromJSON(jsonValue),
    );
  }

  /**
   * Requires locations:write and Idempotency-Key for API keys. Retains the location and historical associations. Archived locations cannot receive new Team assignments. Repeating archive returns the archived location. Changes apply to both Test and Live.
   * Archive location
   */
  async archivePracticeLocation(
    requestParameters: ArchivePracticeLocationRequest,
    initOverrides?: RequestInit | runtime.InitOverrideFunction,
  ): Promise<ArchivePracticeLocationResponse> {
    const response = await this.archivePracticeLocationRaw(requestParameters, initOverrides);
    return await response.value();
  }

  /**
   * Creates request options for createPracticeLocation without sending the request
   */
  async createPracticeLocationRequestOpts(
    requestParameters: CreatePracticeLocationOperationRequest,
  ): Promise<runtime.RequestOpts> {
    if (requestParameters["practiceId"] == null) {
      throw new runtime.RequiredError(
        "practiceId",
        'Required parameter "practiceId" was null or undefined when calling createPracticeLocation().',
      );
    }

    if (requestParameters["idempotencyKey"] == null) {
      throw new runtime.RequiredError(
        "idempotencyKey",
        'Required parameter "idempotencyKey" was null or undefined when calling createPracticeLocation().',
      );
    }

    if (requestParameters["createPracticeLocationRequest"] == null) {
      throw new runtime.RequiredError(
        "createPracticeLocationRequest",
        'Required parameter "createPracticeLocationRequest" was null or undefined when calling createPracticeLocation().',
      );
    }

    const queryParameters: any = {};

    const headerParameters: runtime.HTTPHeaders = {};

    headerParameters["Content-Type"] = "application/json";

    if (requestParameters["affinityVersion"] != null) {
      headerParameters["Affinity-Version"] = String(requestParameters["affinityVersion"]);
    }

    if (requestParameters["idempotencyKey"] != null) {
      headerParameters["Idempotency-Key"] = String(requestParameters["idempotencyKey"]);
    }

    if (this.configuration && this.configuration.accessToken) {
      const token = this.configuration.accessToken;
      const tokenString = await token("bearerAuth", []);

      if (tokenString) {
        headerParameters["Authorization"] = `Bearer ${tokenString}`;
      }
    }
    if (this.configuration && this.configuration.apiKey) {
      headerParameters["x-affinity-api-key"] =
        await this.configuration.apiKey("x-affinity-api-key"); // affinityApiKey authentication
    }

    let urlPath = `/v1/practices/{practiceId}/locations`;
    urlPath = urlPath.replace(
      "{practiceId}",
      encodeURIComponent(String(requestParameters["practiceId"])),
    );

    return {
      path: urlPath,
      method: "POST",
      headers: headerParameters,
      query: queryParameters,
      body: CreatePracticeLocationRequestToJSON(requestParameters["createPracticeLocationRequest"]),
    };
  }

  /**
   * Requires locations:write and Idempotency-Key for API keys. Creates an active location with a unique name in this practice. Locations are shared between Test and Live. Use the returned ID for Team location access.
   * Create location
   */
  async createPracticeLocationRaw(
    requestParameters: CreatePracticeLocationOperationRequest,
    initOverrides?: RequestInit | runtime.InitOverrideFunction,
  ): Promise<runtime.ApiResponse<CreatePracticeLocationResponse>> {
    const requestOptions = await this.createPracticeLocationRequestOpts(requestParameters);
    const response = await this.request(requestOptions, initOverrides);

    return new runtime.JSONApiResponse(response, (jsonValue) =>
      CreatePracticeLocationResponseFromJSON(jsonValue),
    );
  }

  /**
   * Requires locations:write and Idempotency-Key for API keys. Creates an active location with a unique name in this practice. Locations are shared between Test and Live. Use the returned ID for Team location access.
   * Create location
   */
  async createPracticeLocation(
    requestParameters: CreatePracticeLocationOperationRequest,
    initOverrides?: RequestInit | runtime.InitOverrideFunction,
  ): Promise<CreatePracticeLocationResponse> {
    const response = await this.createPracticeLocationRaw(requestParameters, initOverrides);
    return await response.value();
  }

  /**
   * Creates request options for getPracticeLocation without sending the request
   */
  async getPracticeLocationRequestOpts(
    requestParameters: GetPracticeLocationRequest,
  ): Promise<runtime.RequestOpts> {
    if (requestParameters["practiceId"] == null) {
      throw new runtime.RequiredError(
        "practiceId",
        'Required parameter "practiceId" was null or undefined when calling getPracticeLocation().',
      );
    }

    if (requestParameters["locationId"] == null) {
      throw new runtime.RequiredError(
        "locationId",
        'Required parameter "locationId" was null or undefined when calling getPracticeLocation().',
      );
    }

    const queryParameters: any = {};

    const headerParameters: runtime.HTTPHeaders = {};

    if (requestParameters["affinityVersion"] != null) {
      headerParameters["Affinity-Version"] = String(requestParameters["affinityVersion"]);
    }

    if (this.configuration && this.configuration.accessToken) {
      const token = this.configuration.accessToken;
      const tokenString = await token("bearerAuth", []);

      if (tokenString) {
        headerParameters["Authorization"] = `Bearer ${tokenString}`;
      }
    }
    if (this.configuration && this.configuration.apiKey) {
      headerParameters["x-affinity-api-key"] =
        await this.configuration.apiKey("x-affinity-api-key"); // affinityApiKey authentication
    }

    let urlPath = `/v1/practices/{practiceId}/locations/{locationId}`;
    urlPath = urlPath.replace(
      "{practiceId}",
      encodeURIComponent(String(requestParameters["practiceId"])),
    );
    urlPath = urlPath.replace(
      "{locationId}",
      encodeURIComponent(String(requestParameters["locationId"])),
    );

    return {
      path: urlPath,
      method: "GET",
      headers: headerParameters,
      query: queryParameters,
    };
  }

  /**
   * Requires locations:read. Returns one active or archived location in the authorized practice.
   * Read location
   */
  async getPracticeLocationRaw(
    requestParameters: GetPracticeLocationRequest,
    initOverrides?: RequestInit | runtime.InitOverrideFunction,
  ): Promise<runtime.ApiResponse<GetPracticeLocationResponse>> {
    const requestOptions = await this.getPracticeLocationRequestOpts(requestParameters);
    const response = await this.request(requestOptions, initOverrides);

    return new runtime.JSONApiResponse(response, (jsonValue) =>
      GetPracticeLocationResponseFromJSON(jsonValue),
    );
  }

  /**
   * Requires locations:read. Returns one active or archived location in the authorized practice.
   * Read location
   */
  async getPracticeLocation(
    requestParameters: GetPracticeLocationRequest,
    initOverrides?: RequestInit | runtime.InitOverrideFunction,
  ): Promise<GetPracticeLocationResponse> {
    const response = await this.getPracticeLocationRaw(requestParameters, initOverrides);
    return await response.value();
  }

  /**
   * Creates request options for listPracticeLocations without sending the request
   */
  async listPracticeLocationsRequestOpts(
    requestParameters: ListPracticeLocationsRequest,
  ): Promise<runtime.RequestOpts> {
    if (requestParameters["practiceId"] == null) {
      throw new runtime.RequiredError(
        "practiceId",
        'Required parameter "practiceId" was null or undefined when calling listPracticeLocations().',
      );
    }

    const queryParameters: any = {};

    if (requestParameters["limit"] != null) {
      queryParameters["limit"] = requestParameters["limit"];
    }

    if (requestParameters["startingAfter"] != null) {
      queryParameters["startingAfter"] = requestParameters["startingAfter"];
    }

    if (requestParameters["endingBefore"] != null) {
      queryParameters["endingBefore"] = requestParameters["endingBefore"];
    }

    if (requestParameters["status"] != null) {
      queryParameters["status"] = requestParameters["status"];
    }

    const headerParameters: runtime.HTTPHeaders = {};

    if (requestParameters["affinityVersion"] != null) {
      headerParameters["Affinity-Version"] = String(requestParameters["affinityVersion"]);
    }

    if (this.configuration && this.configuration.accessToken) {
      const token = this.configuration.accessToken;
      const tokenString = await token("bearerAuth", []);

      if (tokenString) {
        headerParameters["Authorization"] = `Bearer ${tokenString}`;
      }
    }
    if (this.configuration && this.configuration.apiKey) {
      headerParameters["x-affinity-api-key"] =
        await this.configuration.apiKey("x-affinity-api-key"); // affinityApiKey authentication
    }

    let urlPath = `/v1/practices/{practiceId}/locations`;
    urlPath = urlPath.replace(
      "{practiceId}",
      encodeURIComponent(String(requestParameters["practiceId"])),
    );

    return {
      path: urlPath,
      method: "GET",
      headers: headerParameters,
      query: queryParameters,
    };
  }

  /**
   * Requires locations:read on a practice key or an authorized platform key. Lists active and archived locations by name, with cursor pagination. Use status to filter. Location records are shared between Test and Live for the same practice.
   * List locations
   */
  async listPracticeLocationsRaw(
    requestParameters: ListPracticeLocationsRequest,
    initOverrides?: RequestInit | runtime.InitOverrideFunction,
  ): Promise<runtime.ApiResponse<ListPracticeLocationsResponse>> {
    const requestOptions = await this.listPracticeLocationsRequestOpts(requestParameters);
    const response = await this.request(requestOptions, initOverrides);

    return new runtime.JSONApiResponse(response, (jsonValue) =>
      ListPracticeLocationsResponseFromJSON(jsonValue),
    );
  }

  /**
   * Requires locations:read on a practice key or an authorized platform key. Lists active and archived locations by name, with cursor pagination. Use status to filter. Location records are shared between Test and Live for the same practice.
   * List locations
   */
  async listPracticeLocations(
    requestParameters: ListPracticeLocationsRequest,
    initOverrides?: RequestInit | runtime.InitOverrideFunction,
  ): Promise<ListPracticeLocationsResponse> {
    const response = await this.listPracticeLocationsRaw(requestParameters, initOverrides);
    return await response.value();
  }

  /**
   * Creates request options for updatePracticeLocation without sending the request
   */
  async updatePracticeLocationRequestOpts(
    requestParameters: UpdatePracticeLocationOperationRequest,
  ): Promise<runtime.RequestOpts> {
    if (requestParameters["practiceId"] == null) {
      throw new runtime.RequiredError(
        "practiceId",
        'Required parameter "practiceId" was null or undefined when calling updatePracticeLocation().',
      );
    }

    if (requestParameters["locationId"] == null) {
      throw new runtime.RequiredError(
        "locationId",
        'Required parameter "locationId" was null or undefined when calling updatePracticeLocation().',
      );
    }

    if (requestParameters["idempotencyKey"] == null) {
      throw new runtime.RequiredError(
        "idempotencyKey",
        'Required parameter "idempotencyKey" was null or undefined when calling updatePracticeLocation().',
      );
    }

    if (requestParameters["updatePracticeLocationRequest"] == null) {
      throw new runtime.RequiredError(
        "updatePracticeLocationRequest",
        'Required parameter "updatePracticeLocationRequest" was null or undefined when calling updatePracticeLocation().',
      );
    }

    const queryParameters: any = {};

    const headerParameters: runtime.HTTPHeaders = {};

    headerParameters["Content-Type"] = "application/json";

    if (requestParameters["affinityVersion"] != null) {
      headerParameters["Affinity-Version"] = String(requestParameters["affinityVersion"]);
    }

    if (requestParameters["idempotencyKey"] != null) {
      headerParameters["Idempotency-Key"] = String(requestParameters["idempotencyKey"]);
    }

    if (this.configuration && this.configuration.accessToken) {
      const token = this.configuration.accessToken;
      const tokenString = await token("bearerAuth", []);

      if (tokenString) {
        headerParameters["Authorization"] = `Bearer ${tokenString}`;
      }
    }
    if (this.configuration && this.configuration.apiKey) {
      headerParameters["x-affinity-api-key"] =
        await this.configuration.apiKey("x-affinity-api-key"); // affinityApiKey authentication
    }

    let urlPath = `/v1/practices/{practiceId}/locations/{locationId}`;
    urlPath = urlPath.replace(
      "{practiceId}",
      encodeURIComponent(String(requestParameters["practiceId"])),
    );
    urlPath = urlPath.replace(
      "{locationId}",
      encodeURIComponent(String(requestParameters["locationId"])),
    );

    return {
      path: urlPath,
      method: "PATCH",
      headers: headerParameters,
      query: queryParameters,
      body: UpdatePracticeLocationRequestToJSON(requestParameters["updatePracticeLocationRequest"]),
    };
  }

  /**
   * Requires locations:write and Idempotency-Key for API keys. Updates only supplied fields; null clears optional contact and address fields. Archived locations cannot be updated. Changes apply to both Test and Live.
   * Update location
   */
  async updatePracticeLocationRaw(
    requestParameters: UpdatePracticeLocationOperationRequest,
    initOverrides?: RequestInit | runtime.InitOverrideFunction,
  ): Promise<runtime.ApiResponse<UpdatePracticeLocationResponse>> {
    const requestOptions = await this.updatePracticeLocationRequestOpts(requestParameters);
    const response = await this.request(requestOptions, initOverrides);

    return new runtime.JSONApiResponse(response, (jsonValue) =>
      UpdatePracticeLocationResponseFromJSON(jsonValue),
    );
  }

  /**
   * Requires locations:write and Idempotency-Key for API keys. Updates only supplied fields; null clears optional contact and address fields. Archived locations cannot be updated. Changes apply to both Test and Live.
   * Update location
   */
  async updatePracticeLocation(
    requestParameters: UpdatePracticeLocationOperationRequest,
    initOverrides?: RequestInit | runtime.InitOverrideFunction,
  ): Promise<UpdatePracticeLocationResponse> {
    const response = await this.updatePracticeLocationRaw(requestParameters, initOverrides);
    return await response.value();
  }
}

/**
 * @export
 */
export const ListPracticeLocationsStatusEnum = {
  Active: "active",
  Archived: "archived",
} as const;
export type ListPracticeLocationsStatusEnum =
  (typeof ListPracticeLocationsStatusEnum)[keyof typeof ListPracticeLocationsStatusEnum];
