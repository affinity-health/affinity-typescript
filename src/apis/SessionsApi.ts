/* tslint:disable */
/* eslint-disable */
/**
 * Affinity API
 * Affinity API for practices and software platforms connecting to the pharmacy network.
 *
 * The version of the OpenAPI document: 2026-08-11
 * Contact: support@joinaffinityai.com
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */

import * as runtime from "../runtime";
import {
  type CreateComponentSessionRequest,
  CreateComponentSessionRequestFromJSON,
  CreateComponentSessionRequestToJSON,
} from "../models/CreateComponentSessionRequest";
import {
  type CreateComponentSessionResponse,
  CreateComponentSessionResponseFromJSON,
  CreateComponentSessionResponseToJSON,
} from "../models/CreateComponentSessionResponse";
import {
  type CreateHostedSessionRequest,
  CreateHostedSessionRequestFromJSON,
  CreateHostedSessionRequestToJSON,
} from "../models/CreateHostedSessionRequest";
import {
  type CreateHostedSessionResponse,
  CreateHostedSessionResponseFromJSON,
  CreateHostedSessionResponseToJSON,
} from "../models/CreateHostedSessionResponse";
import { type Problem, ProblemFromJSON, ProblemToJSON } from "../models/Problem";

export interface CreateComponentSessionOperationRequest {
  idempotencyKey: string;
  createComponentSessionRequest: CreateComponentSessionRequest;
  affinityVersion?: string;
}

export interface CreateHostedSessionOperationRequest {
  idempotencyKey: string;
  createHostedSessionRequest: CreateHostedSessionRequest;
  affinityVersion?: string;
}

/**
 *
 */
export class SessionsApi extends runtime.BaseAPI {
  /**
   * Creates request options for createComponentSession without sending the request
   */
  async createComponentSessionRequestOpts(
    requestParameters: CreateComponentSessionOperationRequest,
  ): Promise<runtime.RequestOpts> {
    if (requestParameters["idempotencyKey"] == null) {
      throw new runtime.RequiredError(
        "idempotencyKey",
        'Required parameter "idempotencyKey" was null or undefined when calling createComponentSession().',
      );
    }

    if (requestParameters["createComponentSessionRequest"] == null) {
      throw new runtime.RequiredError(
        "createComponentSessionRequest",
        'Required parameter "createComponentSessionRequest" was null or undefined when calling createComponentSession().',
      );
    }

    const queryParameters: any = {};

    const headerParameters: runtime.HTTPHeaders = {};

    headerParameters["Content-Type"] = "application/json";

    if (requestParameters["affinityVersion"] != null) {
      headerParameters["Affinity-Version"] = String(requestParameters["affinityVersion"]);
    }

    if (requestParameters["idempotencyKey"] != null) {
      headerParameters["Idempotency-Key"] = String(requestParameters["idempotencyKey"]);
    }

    if (this.configuration && this.configuration.accessToken) {
      const token = this.configuration.accessToken;
      const tokenString = await token("bearerAuth", []);

      if (tokenString) {
        headerParameters["Authorization"] = `Bearer ${tokenString}`;
      }
    }
    if (this.configuration && this.configuration.apiKey) {
      headerParameters["x-affinity-api-key"] =
        await this.configuration.apiKey("x-affinity-api-key"); // affinityApiKey authentication
    }

    let urlPath = `/v1/component-sessions`;

    return {
      path: urlPath,
      method: "POST",
      headers: headerParameters,
      query: queryParameters,
      body: CreateComponentSessionRequestToJSON(requestParameters["createComponentSessionRequest"]),
    };
  }

  /**
   * Creates a one-time, origin-bound Affinity Elements client secret. The platform API key must stay on the server.
   * Create component session
   */
  async createComponentSessionRaw(
    requestParameters: CreateComponentSessionOperationRequest,
    initOverrides?: RequestInit | runtime.InitOverrideFunction,
  ): Promise<runtime.ApiResponse<CreateComponentSessionResponse>> {
    const requestOptions = await this.createComponentSessionRequestOpts(requestParameters);
    const response = await this.request(requestOptions, initOverrides);

    return new runtime.JSONApiResponse(response, (jsonValue) =>
      CreateComponentSessionResponseFromJSON(jsonValue),
    );
  }

  /**
   * Creates a one-time, origin-bound Affinity Elements client secret. The platform API key must stay on the server.
   * Create component session
   */
  async createComponentSession(
    requestParameters: CreateComponentSessionOperationRequest,
    initOverrides?: RequestInit | runtime.InitOverrideFunction,
  ): Promise<CreateComponentSessionResponse> {
    const response = await this.createComponentSessionRaw(requestParameters, initOverrides);
    return await response.value();
  }

  /**
   * Creates request options for createHostedSession without sending the request
   */
  async createHostedSessionRequestOpts(
    requestParameters: CreateHostedSessionOperationRequest,
  ): Promise<runtime.RequestOpts> {
    if (requestParameters["idempotencyKey"] == null) {
      throw new runtime.RequiredError(
        "idempotencyKey",
        'Required parameter "idempotencyKey" was null or undefined when calling createHostedSession().',
      );
    }

    if (requestParameters["createHostedSessionRequest"] == null) {
      throw new runtime.RequiredError(
        "createHostedSessionRequest",
        'Required parameter "createHostedSessionRequest" was null or undefined when calling createHostedSession().',
      );
    }

    const queryParameters: any = {};

    const headerParameters: runtime.HTTPHeaders = {};

    headerParameters["Content-Type"] = "application/json";

    if (requestParameters["affinityVersion"] != null) {
      headerParameters["Affinity-Version"] = String(requestParameters["affinityVersion"]);
    }

    if (requestParameters["idempotencyKey"] != null) {
      headerParameters["Idempotency-Key"] = String(requestParameters["idempotencyKey"]);
    }

    if (this.configuration && this.configuration.accessToken) {
      const token = this.configuration.accessToken;
      const tokenString = await token("bearerAuth", []);

      if (tokenString) {
        headerParameters["Authorization"] = `Bearer ${tokenString}`;
      }
    }
    if (this.configuration && this.configuration.apiKey) {
      headerParameters["x-affinity-api-key"] =
        await this.configuration.apiKey("x-affinity-api-key"); // affinityApiKey authentication
    }

    let urlPath = `/v1/hosted-sessions`;

    return {
      path: urlPath,
      method: "POST",
      headers: headerParameters,
      query: queryParameters,
      body: CreateHostedSessionRequestToJSON(requestParameters["createHostedSessionRequest"]),
    };
  }

  /**
   * Creates a hosted workflow URL that expires after 15 minutes. Use order_review to open an existing draft for clinician signing in Clinic. Opening the page does not change order state; no review request or approval step is required. The clinician must sign in and have current prescribing access; the link grants no signing authority. Keep the practice or platform API key on the server.
   * Create hosted session
   */
  async createHostedSessionRaw(
    requestParameters: CreateHostedSessionOperationRequest,
    initOverrides?: RequestInit | runtime.InitOverrideFunction,
  ): Promise<runtime.ApiResponse<CreateHostedSessionResponse>> {
    const requestOptions = await this.createHostedSessionRequestOpts(requestParameters);
    const response = await this.request(requestOptions, initOverrides);

    return new runtime.JSONApiResponse(response, (jsonValue) =>
      CreateHostedSessionResponseFromJSON(jsonValue),
    );
  }

  /**
   * Creates a hosted workflow URL that expires after 15 minutes. Use order_review to open an existing draft for clinician signing in Clinic. Opening the page does not change order state; no review request or approval step is required. The clinician must sign in and have current prescribing access; the link grants no signing authority. Keep the practice or platform API key on the server.
   * Create hosted session
   */
  async createHostedSession(
    requestParameters: CreateHostedSessionOperationRequest,
    initOverrides?: RequestInit | runtime.InitOverrideFunction,
  ): Promise<CreateHostedSessionResponse> {
    const response = await this.createHostedSessionRaw(requestParameters, initOverrides);
    return await response.value();
  }
}
