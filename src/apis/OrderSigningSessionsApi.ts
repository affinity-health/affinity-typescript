/* tslint:disable */
/* eslint-disable */
/**
 * Affinity API
 * Affinity API for software platforms connecting practices to the compounder network. A practice is the customer organization, a provider is an individual clinician or prescriber, and a location is a physical practice site. The API covers practice management, catalog discovery, prescription-order submission, fulfillment tracking, and webhooks.
 *
 * The version of the OpenAPI document: 2026-07-29
 * Contact: support@joinaffinityai.com
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */

import * as runtime from "../runtime";
import {
  type CreateOrderSigningSessionRequest,
  CreateOrderSigningSessionRequestFromJSON,
  CreateOrderSigningSessionRequestToJSON,
} from "../models/CreateOrderSigningSessionRequest";
import {
  type CreateOrderSigningSessionResponse,
  CreateOrderSigningSessionResponseFromJSON,
  CreateOrderSigningSessionResponseToJSON,
} from "../models/CreateOrderSigningSessionResponse";
import { type Problem, ProblemFromJSON, ProblemToJSON } from "../models/Problem";

export interface CreateOrderSigningSessionOperationRequest {
  createOrderSigningSessionRequest: CreateOrderSigningSessionRequest;
  idempotencyKey?: string;
  affinityVersion?: string;
}

/**
 *
 */
export class OrderSigningSessionsApi extends runtime.BaseAPI {
  /**
   * Creates request options for createOrderSigningSession without sending the request
   */
  async createOrderSigningSessionRequestOpts(
    requestParameters: CreateOrderSigningSessionOperationRequest,
  ): Promise<runtime.RequestOpts> {
    if (requestParameters["createOrderSigningSessionRequest"] == null) {
      throw new runtime.RequiredError(
        "createOrderSigningSessionRequest",
        'Required parameter "createOrderSigningSessionRequest" was null or undefined when calling createOrderSigningSession().',
      );
    }

    const queryParameters: any = {};

    const headerParameters: runtime.HTTPHeaders = {};

    headerParameters["Content-Type"] = "application/json";

    if (requestParameters["idempotencyKey"] != null) {
      headerParameters["Idempotency-Key"] = String(requestParameters["idempotencyKey"]);
    }

    if (requestParameters["affinityVersion"] != null) {
      headerParameters["Affinity-Version"] = String(requestParameters["affinityVersion"]);
    }

    if (this.configuration && this.configuration.accessToken) {
      const token = this.configuration.accessToken;
      const tokenString = await token("bearerAuth", []);

      if (tokenString) {
        headerParameters["Authorization"] = `Bearer ${tokenString}`;
      }
    }
    if (this.configuration && this.configuration.apiKey) {
      headerParameters["x-affinity-api-key"] =
        await this.configuration.apiKey("x-affinity-api-key"); // affinityApiKey authentication
    }

    let urlPath = `/v1/order-signing-sessions`;

    return {
      path: urlPath,
      method: "POST",
      headers: headerParameters,
      query: queryParameters,
      body: CreateOrderSigningSessionRequestToJSON(
        requestParameters["createOrderSigningSessionRequest"],
      ),
    };
  }

  /**
   * Creates a one-time Affinity Hosted URL where the mapped provider reviews and PIN-signs every prescription in one patient order. The provider PIN never reaches the platform.
   * Create order signing session
   */
  async createOrderSigningSessionRaw(
    requestParameters: CreateOrderSigningSessionOperationRequest,
    initOverrides?: RequestInit | runtime.InitOverrideFunction,
  ): Promise<runtime.ApiResponse<CreateOrderSigningSessionResponse>> {
    const requestOptions = await this.createOrderSigningSessionRequestOpts(requestParameters);
    const response = await this.request(requestOptions, initOverrides);

    return new runtime.JSONApiResponse(response, (jsonValue) =>
      CreateOrderSigningSessionResponseFromJSON(jsonValue),
    );
  }

  /**
   * Creates a one-time Affinity Hosted URL where the mapped provider reviews and PIN-signs every prescription in one patient order. The provider PIN never reaches the platform.
   * Create order signing session
   */
  async createOrderSigningSession(
    requestParameters: CreateOrderSigningSessionOperationRequest,
    initOverrides?: RequestInit | runtime.InitOverrideFunction,
  ): Promise<CreateOrderSigningSessionResponse> {
    const response = await this.createOrderSigningSessionRaw(requestParameters, initOverrides);
    return await response.value();
  }
}
